# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(phins_ixseaTaskLib)
ADD_LIBRARY(${PHINS_IXSEA_TASKLIB_NAME} SHARED 
    ${PHINS_IXSEA_TASKLIB_SOURCES})

add_dependencies(${PHINS_IXSEA_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${PHINS_IXSEA_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PHINS_IXSEA_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PHINS_IXSEA_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PHINS_IXSEA_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PHINS_IXSEA_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PHINS_IXSEA_TASKLIB_HEADERS}
    DESTINATION include/orocos/phins_ixsea)

